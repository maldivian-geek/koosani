/// <reference types="vite/client" />

// Injected at build time via vite.config.ts `define` — the package.json
// version, kept in lockstep with CHANGELOG releases.
declare const __APP_VERSION__: string
