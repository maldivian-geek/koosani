VITE_API_URL=http://localhost:3000

# PrimeUI community license key (JWT), required since PrimeVue 5. Build-time
# only - baked into the JS bundle, not a server secret. Without it PrimeVue
# shows a small "Invalid PrimeUI License" corner banner; nothing breaks.
VITE_PRIMEVUE_LICENSE_KEY=
